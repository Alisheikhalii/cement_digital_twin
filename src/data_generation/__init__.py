"""Synthetic dataset builders (PRD v1.1.1 Sections 11.2, 11.6, 12).

Drives ``ScenarioScheduler`` -> ``Twin.simulate_scenario`` -> ``SensorModel`` and exports the
kiln/mill datasets to CSV + Parquet with the JSON config sidecar (FR-4, FR-1).
"""
