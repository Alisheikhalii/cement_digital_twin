"""Process-model test suite (PRD v1.1.1 Section 34).

A package rather than a bare directory so the modules can share the constants defined in
:mod:`tests.conftest` (the simulation step and the test horizon) instead of each restating them.
"""
